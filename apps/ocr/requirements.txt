# Pinned for reproducibility between local dev and Hetzner (both CPU-only —
# see PaddleOCR init flags in app/ocr_engine.py for why GPU is never assumed).
# Versions verified installable as of this batch (2026-09-23); bump
# deliberately, not automatically, and re-verify the paddleocr predict()
# output shape in app/ocr_engine.py if paddleocr's major version changes.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
paddlepaddle==3.2.2
paddleocr==3.7.0
pymupdf==1.25.2
pillow==11.1.0
